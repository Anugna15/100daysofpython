def vowel(string):
    count = 0
    for char in string:
        if char.lower() in 'aeiou':
            count += 1
    return count
string = input("Enter a string: ")
print("Number of vowels in the string is:", vowel(string))